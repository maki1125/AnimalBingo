<!DOCTYPE html>
<html>
<head>

  <%= javascript_tag do %>
    document.addEventListener('turbo:load', function() {
      Turbo.session.drive = false;
    });
  <% end %>
  <meta charset="utf-8" /> 
  <title>collection</title>
  <%= stylesheet_link_tag 'collection_index', :media => "all" %>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
  <% content_for :js do %>
    <%= javascript_import_module_tag "index" %>
  <% end %>
</head>
<body>
  <div class="container">
    <h1>コレクション</h1>
    <p><span style="color:red;">クイズ</span>に挑戦して『？』を仲間にしよう。
    <br>動物を<span style="color:red;">クリック</span>すると動画が見えるよ！
    
    </p>
    <div class="btnWrapper">
      <button class="btn1" id="どうぶつ">どうぶつ🐕</button>
      <button class="btn1" id="さかな">さかな🐟</button>
      <button class="btn1" id="きょうりゅう">きょうりゅう🦕</button>
    </div>
    <div class="col_card"  id="col_card">
    </div>

    <div class="btnWrapper">
      <button class="btn2" id="pg1">1</button>
      <button class="btn2">2</button>
      <button class="btn2">3</button>
      <button class="btn2">4</button>
      <button class="btn2">5</button>
    </div>
  </div>
  
  <script> //javascriptにデータを渡す。
    pic_mode = "<%= @favorite_btn %>"
    //1.動物
    allanimal_imgass = <%= raw @allanimal_img.map { |path| asset_path("#{path}") }.to_json %>;
    colanimal_img = <%= raw @colanimal_img.map { |path| "#{path}" }.to_json %>;
    colanimal_imgass = <%= raw @colanimal_img.map { |path| asset_path("#{path}") }.to_json %>;
    animal_quiz = <%= @quiz.animal_quiz %>

    //2.魚
    allfish_imgass = <%= raw @allfish_img.map { |path| asset_path("#{path}") }.to_json %>;
    colfish_img = <%= raw @colfish_img.map { |path| "#{path}" }.to_json %>;
    colfish_imgass = <%= raw @colfish_img.map { |path| asset_path("#{path}") }.to_json %>;
    fish_quiz = <%= @quiz.fish_quiz %>//仲間の数

    //3.恐竜
    alldinosaur_imgass = <%= raw @alldinosaur_img.map { |path| asset_path("#{path}") }.to_json %>;
    coldinosaur_img = <%= raw @coldinosaur_img.map { |path| "#{path}" }.to_json %>;
    coldinosaur_imgass = <%= raw @coldinosaur_img.map { |path| asset_path("#{path}") }.to_json %>;
    dinosaur_quiz = <%= @quiz.dinosaur_quiz %>//仲間の数

    //はてなマーク
    question_imgass = "<%= asset_path("question101.webp") %>";
    page2 = <%= @page %>
    </script>

</body>
</html>